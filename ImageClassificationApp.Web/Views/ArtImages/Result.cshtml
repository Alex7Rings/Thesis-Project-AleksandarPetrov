@using ImageClassificationApp_Web
@model MLModel_Art.ModelOutput

@{
    ViewData["Title"] = "Classification Result";
    var imagePath = ViewData["ImagePath"] as string;
    var accuracy = ViewData["Accuracy"] as float?;
}

<div class="result-container">
    <h1>Резултат от класификацията</h1>

    <div class="result-details">
        <p><strong>Прогнозa:</strong> @Model.PredictedLabel</p>

        @if (accuracy.HasValue)
        {
            <p><strong>Точност на прогнозата:</strong> @(Math.Round(accuracy.Value * 100))%</p>
        }
    </div>

    @if (!string.IsNullOrEmpty(imagePath))
    {
        <div class="image-container">
            <h2>Заредено изображение:</h2>
            <img src="@imagePath" alt="Заредено изображение" class="uploaded-image" />
        </div>
    }
</div>
